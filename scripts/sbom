#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)
source "$SCRIPT_DIR/common.sh"
ensure_venv

mkdir -p "$REPO_ROOT/sbom"
run cyclonedx-py --spec-version 1.4 --format json --output "$REPO_ROOT/sbom/sbom.json"
